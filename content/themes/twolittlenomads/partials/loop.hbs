{{! This is the post loop - each post will be output using this markup }}
{{#foreach posts}}
<article id="{{url}}" class="blog-overview row">
    <div class="row-same-height">
        {{#if @odd}}
        <div class="col-md-6 col-same-height col-align-middle title" style="background-image: url({{image}})">
            <div class="overview-content">
                <a href="{{url}}" class="title-read-link">
                    <h2>Read</h2>
                </a>  
            </div>
        </div>
        <div class="col-md-6 col-same-height col-align-middle excerpt">
            <div class="overview-content">
                <h2>{{title}}</h2>
                <hr></hr>
                <p>{{excerpt words="75"}} ...</p>
                <hr></hr>
                <a href="{{url}}" class="read-link">READ</a> 
            </div>
        </div>
        {{else}}
        <div class="col-md-6 col-same-height col-align-middle excerpt">
            <div class="overview-content">
                <h2>{{title}}</h2>
                <hr></hr>
                <p>{{excerpt words="75"}} ...</p>
                <hr></hr>
                <a href="{{url}}" class="read-link">READ</a> 
            </div>
        </div>
        <div class="col-md-6 col-same-height col-align-middle title" style="background-image: url({{image}})">
            <div class="overview-content">
                <a href="{{url}}" class="title-read-link">
                    <h2>Read</h2>
                </a>    
            </div>
        </div>
        {{/if}}
    </div>

<!--    {{#if image}}
        <div class="post-header-image" style="background-image: url({{image}})"></div>
    {{else}}
        <hr class="post-date-line" />
    {{/if}}
    <time class="post-date with-line post-meta-text" datetime="{{date format='YYYY-MM-DD'}}">{{date format="DD MMMM YYYY"}}</time>
    <div class="{{post_class}}">
        <i class="post-meta-text post-meta-icon fa fa-heart fa-2x"></i>
        <div class="post-meta post-meta-text tags">
            {{tags}}
        </div>
        <hr class="post-header-line top"/>
        <header class="post-header">
            <h2 class="post-title"><a href="#{{url}}">{{title}}</a></h2>
        </header>
        <hr class="post-header-line bottom"/>
        <section class="post-content">
            {{content}}
        </section>
    </div>-->
    
</article>
{{/foreach}}

{{! Previous/next page links - displayed on every page }}
{{pagination}}    

